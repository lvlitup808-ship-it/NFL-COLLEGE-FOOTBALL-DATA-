# Weekly Meta-Loop Proposal

This file is a proposal only. It has no executable runner.

Once per week, one worker may read append-only records in `40-runs/` and propose a patch to `10-SKILL.md` or `50-CONSTRAINTS.md`.

The worker must not apply, commit, publish, or merge that patch.

Every proposed line must cite the run record that caused it. One evidenced mistake per constraint line.

The human must type `APPROVE` before any edit.

Customer, buyer, school, and quote: NOT IN THE EVIDENCE.
