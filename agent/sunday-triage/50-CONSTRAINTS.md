- Never write program_play_tags from ingest.
- Never promote source_play_class to play_family.
- Never treat n<8 as a tendency.
- Never invent customer quotes.
- Append new constraints only when a human pastes a correction.

UNKNOWN counts read play-row defaults (plays.play_family, plays.formation), never resolved program_play_tags.

Both UNKNOWN-count columns exist on `plays`; no tag-table fallback is required. [FACT — `db/schema.sql`]

- Never write `play_tag_votes`.
- Never ingest or invoke an ingest script.
- Never dump raw CFBD/nflverse rows.
- Never freeze a week plan.
- Never email, tweet, pay, send, or publish.
- Never write `40-runs/`; the human gate owns that append-only copy.
- Never schedule this loop.
- Never create `customer.md`, customer, buyer, school, or quote nodes.
- Never edit this file without a written human `APPROVE`.
